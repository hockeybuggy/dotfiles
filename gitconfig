[user]
	name = Douglas Anderson
	email = dougjamesanderson@gmail.com

[color]
	branch = auto
	diff = auto
	status = auto

[alias]
# Basic action aliases
	ci   = commit
	com  = commit -m
	cam  = commit -am
	sta  = status -s
# Log aliases
	lg   = log --oneline
# Branching aliases
	bra  = branch
	che  = checkout
# Push-Pull aliases
	pom  = push origin master
	plo  = pull origin master
# Cloneing aliases 
	cl   = clone
	clo  = clone
	clgh = clone gh:# gh is a ssh host
	clogh= clone gh:

