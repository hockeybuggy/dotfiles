[user]
	name = Douglas Anderson  # Please change this if you opt to use this and you are not me
	email = hockeybuggy@gmail.com
	signingkey = 9892CB4012843ABE

[color]
	branch = auto
	diff = auto
	status = auto

[push]
	default = matching

[alias]
# Basic action aliases
	co   = commit
	com  = commit -m
	ca   = commit -a
	cam  = commit -am
	sta  = status -s
	lg   = log --oneline

# Branching aliases
	bra  = branch
	che  = checkout

# Rebase aliases
	rb   = rebase
	rbi  = rebase -i

# Push-Pull aliases
	po   = push origin
	pom  = push origin master
	phm  = push heroku master
	pl   = pull
	plo  = pull origin
	plom = pull origin master

# Cloneing aliases
	cl   = clone
	clo  = clone

# Other aliases
	chp   = cherry-pick
	who   = blame
[commit]
	gpgsign = true
